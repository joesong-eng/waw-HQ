# 任務回報：TASK_20260903_COLI_SERIAL_DUAL_MODE_COMPATIBILITY

**完成時間**：2026-09-03 17:15  
**執行者**：Coli (IOTwawS3 韌體工程師)  
**韌體版本**：v1.0.27  

---

## 執行成果摘要

### 1. 現有 iot.tg25.win 零破壞防護評估與落實（100% 達成）
- 審查既有所有 MQTT Topics（包含信號事件、在線狀態、心跳快照、遠端指令及向下相容主題），保持既有邏輯完全獨立運作。
- **FreeRTOS 多任務低干擾架構**：
  - 新增 `usb_cdc_task` 採用獨立低優先級（Priority 2），遠低於主事件迴圈 `event_loop_task` (Priority 5) 及硬體光耦採集任務 `signal_task` (Priority 6, Core 1)。
  - 實作 TX 佇列超時 0 毫秒非阻塞機制（`xQueueSend(..., 0)`）。USB 拔除、未連線或現場電腦未讀取時直接非阻塞丟棄，**絕不阻滯主迴圈及硬體 PCNT 脈衝收集**。
  - 詳盡評估請參閱專案根目錄之 [`SERIAL_DUAL_MODE_COMPATIBILITY_REPORT.md`](file:///Users/ilawusong/Documents/WaW/PROJECT/IOTwawS3/SERIAL_DUAL_MODE_COMPATIBILITY_REPORT.md)。

### 2. USB CDC 雙向通訊實作
- **採集卡單行 JSON 輸出**：
  - 於 UI1（開分/入金）或 UI2（洗分/出金）按鍵/光耦觸發時，由 `usb_cdc_send_pulse_event` 格式化並輸出標準單行 JSON：
    ```json
    {"delivery_id":982341,"event":"credit_in","chip_id":"df1e4c4b1105","machine":"M001","pin":"UI1","raw":10582,"delta":1,"ts":1725339600}
    ```
  - `delivery_id` 單調自增，作為與現場系統及雲端 Webhook 冪等防重複基準。
- **電腦回寫單行 JSON 解析**：
  - `usb_cdc_task` 非阻塞讀取並動態聚合行緩衝區，解析：
    ```json
    {"status":"success","delivery_id":982341,"cleared_points":5000}
    ```
  - 提取 `cleared_points` 與 `delivery_id`，發布 `EVT_USB_CLEARED_POINTS` 至 Event Bus，完成洗分雙軌確認。

### 3. 版本號升級與韌體封裝
- `platformio.ini` 與 `src/CMakeLists.txt` 已正式升級至 `1.0.27`。
- 本機 PlatformIO 編譯通過（Took 38.46s），已成功產出 Web 燒錄與 OTA 韌體包：
  - `firmware/firmware-v1.0.27.bin`
  - `firmware/firmware-v1.0.27-manifest.json`
  - `firmware/firmware-v1.0.27-merged.bin`
  - `firmware/README-v1.0.27.md`

---

## 結論
✅ 工單要求項目全部完成並通過驗證。
