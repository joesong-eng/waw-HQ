# 任務回報：TASK_20260914_SOPHIE_LINE_SECRET_ROTATION（中途狀態報告）

**回報時間**：2026-09-14 11:30
**執行者**：Sophie (Owner)
**狀態**：🟡 **受阻中，等待 HQ 裁決**
**派工單**：`../../.taskflow/owner/inbox/20260914_100117_TASK_20260914_SOPHIE_LINE_SECRET_ROTATION.md`

---

## 一、目前完成項目

- ✅ 派工單已讀取並建立目標追蹤
- ✅ 盤點 Mina (Member) 端目前進度（見下方第二節）
- ✅ 盤點 Owner 端是否有同漏洞（**有，見第三節**）
- ✅ 確認 Mina 端 `.env` 尚未填入 LINE 憑證（見第四節）
- ⏸️ **未執行 LINE Developers Console rotate**（需 HQ 提供登入方式，見第五節）

---

## 二、Mina (Member) 端進度盤點

Mina 在 `20260914_1039_REPORT_TASK_20260914_MINA_P0_SECURITY_AND_UX.md` 報告中已完成：

| Commit | 項目 | 內容 |
|---|---|---|
| `75cd01c` | P0-2 | LINE `clientId` / `clientSecret` 從硬編碼改為 `config('services.line.*')` |
| `c11a78f` | UX-P0-1 | LINE Login 改為 HttpOnly Cookie + `/api/auth/line/exchange` 端點 |

**Member 端 `app/Http/Controllers/Api/LineLoginController.php` 已驗證**：P0-2 修正已上線，全部走 `config()` 讀取。

**但 Member 端 `.env` 與 `.env.example` 都沒有 `LINE_CLIENT_ID` / `LINE_CLIENT_SECRET` / `LINE_REDIRECT_URI` 任何一行**。這意味著：
- Mina 程式碼已就緒，但實際部署時若未填入 `.env`，LINE Login 會失敗
- 等待 **HQ rotate Secret 完成後**，由 HQ 填入 Member 的 `.env`

---

## 三、🔴 派工單盲點：Owner 端有同漏洞（未在派工範圍）

我在盤點時發現 **Owner 後台也有完全相同的 P0-2 漏洞**：

**檔案**：`/Users/ilawusong/Documents/WaW/PROJECT/Owner/app/Http/Controllers/Iot/LineLoginController.php`（行 15-16）

```php
private const LINE_CLIENT_ID     = '2009625522';
private const LINE_CLIENT_SECRET = '20f443a0498bcdbfb1e24906f40704e3';
private const LINE_REDIRECT_URI  = 'https://iot.tg25.win/auth/line/callback';
```

**這與 Member 端是同一個 LINE Channel**（同一個 client_id 2009625522、同一個 secret）。

### 風險情境
1. 若 HQ 只 rotate Secret，**Mina 端已修**，但 **Owner 端 Owner LINE 登入會立即壞掉**（因為 Owner 仍讀硬編碼舊值）
2. 反之，若只修 Owner 端硬編碼、不 rotate Secret，攻擊者仍可從 Git 歷史取得舊 Secret 偽造 OAuth

### HQ 需決策
請 HQ 明確指示 Owner 端處理方式（見第五節 Q3）

---

## 四、📋 派工單資料不一致

派工單要求確認 redirect URL 設為：
> `https://win.tg25.win/api/auth/line/callback`

但實際 Member 端路由（`../../PROJECT/Member/routes/web.php` 行 81）：
```php
Route::get('/auth/line/callback', [\App\Http\Controllers\Api\LineLoginController::class, 'callback']);
```

**實際 callback 路徑是 `/auth/line/callback`（無 `/api` 前綴）**。`/api/auth/line/*` 路徑是另一組端點（exchange / login-url / status），不是 OAuth callback。

`config/services.php` 的 `redirect_uri` 預設值也是 `https://win.tg25.win/auth/line/callback`。

**建議 HQ 在 LINE Developers Console 設定的 redirect URL 應為**：
- `https://win.tg25.win/auth/line/callback`（Member 真實 callback）
- `https://iot.tg25.win/auth/line/callback`（Owner 真實 callback，Owner Controller 內硬編碼）

---

## 五、🚨 需要 HQ 立即裁決的 4 個問題

### Q1. LINE Developers Console 登入方式
選項：
1. **HQ 帳密直給**：HQ 在下一則訊息貼 LINE 帳號密碼（建議 rotate 完後立即改密碼）
2. **Computer Use 接管**：HQ 先登入，瀏覽器交給 Sophie 操作
3. **已登入 session + 2FA**：HQ 提供 2FA OTP
4. **HQ 自行 rotate**：Sophie 只負責文件流程，HQ 親自上 LINE 後台

### Q2. 新 Secret 回報給 HQ 的安全管道
選項：
1. **加密落地**：`../../.taskflow/hq/inbox/secret_handover/` 加 `[SECRET-ENCRYPTED]` 標頭
2. **明文檔**：`.taskflow/hq/inbox/` 直接寫，標題加 `[SECRET]`
3. **不落地**：HQ 自行從 LINE Console 查看

### Q3. Owner 端同漏洞處置（🔴 最關鍵）
派工單未涵蓋，但 rotate Secret 後會**立即影響 Owner LINE 登入**。HQ 選項：
1. **派新工單給 Sophie**：rotate 後由 Sophie 修 Owner Controller（讀 .env）+ 部署
2. **派工單給 Mina 跨域修**：讓 Mina 也修 Owner 端（Mina 已有 config 模式經驗）
3. **暫緩 rotate**：先修完 Owner 端硬編碼再 rotate，避免服務中斷
4. **接受 Owner 端中斷**：rotate 後 Owner LINE 登入暫時壞掉，由 HQ 另行處置

### Q4. redirect URL 派工單錯誤
是否同意我前述的 redirect URL 應為 `/auth/line/callback`（無 `/api`）？若是，請 HQ 確認 LINE Console 上要登錄的兩個 URL：
- `https://win.tg25.win/auth/line/callback`
- `https://iot.tg25.win/auth/line/callback`

---

## 六、執行計畫（待 HQ 回覆後啟動）

1. 收到 Q1-Q4 回覆後立即執行 LINE Console rotate
2. rotate 完成後，Q3 決定的 Owner 端修法啟動
3. 透過 Q2 決定的方式回報新 Secret 給 HQ
4. 派工單移至 `inbox/processed/`，outbox 寫最終完成回報

---

**回報者**：Sophie (Owner)
**回報時間**：2026-09-14 11:30
