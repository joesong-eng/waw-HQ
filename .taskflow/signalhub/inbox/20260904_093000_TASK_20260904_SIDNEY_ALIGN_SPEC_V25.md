
# 審查與對齊指令：TASK_20260904_SIDNEY_ALIGN_SPEC_V25

**發送時間**：2026-09-04 09:30  
**發送者**：HQ  
**對象**：Sidney (SignalHub Lead)  
**標準依據**：`brains/knowledge/03_system_architecture/SIGNALHUB_HARDWARE_WEBHOOK_INTEGRATION_SPEC_v2.md` (v2.5)

---

## 🔍 HQ 審查發現與需對齊項目

Sidney，剛才 HQ 審查了你提交的 `795fb36` 成果，基礎建設與 10 秒重試、日誌機制完成得很好！
但在今天早上的最新業務討論中，HQ 與老李/小猴確定了幾項關鍵更新（v2.5 標準），請你逐項對齊：

### 1. 廢除固定倍率，開洗分全面改用 `actual_points` 回報
- **現狀**：你的精靈頁面還有「倍率換算」。
- **對齊要求**：
  1. 精靈 UI 移除倍率輸入框，UI1/UI2 僅作為功能定義（UI1=開分，UI2=洗分）。
  2. 開分 (credit_in) 與洗分 (credit_out) 回覆均解析小猴回傳的 `actual_points`（純整數 BIGINT）。
  3. `signal_webhook_deliveries` 資料表與 Deliveries 頁面，統一展示 `actual_points`（實際開洗分數值）。

### 2. 小猴主動上報「分數歸零 (session_end)」API
- **對齊要求**：
  - 新增端點：`POST /api/v9/signal-hub/inbound/session-end`
  - Headers 驗證：`X-WAW-Secret: {secret_key}`
  - 接收 Payload：`{ chip_id, machine_number, event: "session_end", reason: "points_cleared_to_zero", last_played_duration_seconds, occurred_at }`
  - 寫入事件日誌，並更新機台狀態為空機閒置。

### 3. 三步精靈 UI 微調
- 移除「一鍵帶入測試接收端」等用戶不需要的測試輔助，回歸純粹的「自訂 Webhook URL」與「USB Serial 開關」。

---

## 📝 請回報對齊確認

請檢視以上三項，並於完成更新後產出對齊確認報告回報至 outbox。

