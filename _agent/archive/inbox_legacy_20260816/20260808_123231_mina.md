# Mina (Member) Task Report: SYS_SYNC_001

- **Task ID**: SYS_SYNC_001
- **Status**: Completed
- **Timestamp**: 2026-08-08 12:15:00 (Asia/Taipei)
- **Agent**: Mina (Member)

## 派工系統運作規則理解 (Dispatch System Rules Understanding)

1. **對話驅動與一次性生命週期**：
   - 所有的 CLI Agent 均非持續常駐程式，而是每次依需啟動的獨立 Session。
   - 啟動後由專案內的 `AGENTS.md`、`_agent/IDENTITY.md` 以及 `_agent/inbox/<task_id>.json` 來讀取任務細節及限制，執行完畢後隨即產出報告並結束。

2. **兩階段派工限制（Consultation & Execution）**：
   - 凡涉及程式異動、資料庫變更、部署調整或跨專案協作者，強制執行**「先諮詢，後執行」**原則。
   - 第一階段 (`consultation`)：分析需求，產出 `consultation_report.json` (狀態為 `awaiting_approval`)。期間不得修改業務程式或基礎設施。
   - 第二階段 (`execution`)：在 HQ 核准方案後，方能啟動 execution，依照核准的計畫實作，並提交驗收證據。

3. **任務自足與持久化**：
   - 任務 JSON 檔包含全部必要上下文與路徑，不依賴前一次 Session 的隱性記憶。
   - 任務與報告透過檔案與佇列（如 Redis Streams）完成異步追蹤與狀態更新。

4. **安全協議**：
   - 啟動時嚴格禁止遞迴掃描父目錄、`vendor/`、`node_modules/` 等大型目錄。
   - 依循技術命名規範及金流冪等性，超出職責範圍應立即向上級（HQ）回報。
